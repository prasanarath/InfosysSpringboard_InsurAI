server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/insurai_db
    username: root
    password: Mysql@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
